#include <iostream>
#include<bits/stdc++.h>
#include<algorithm>
#define pb push_back
#define mp make_pair
#define pii pair< int, int >
#define pll pair< long long, long long >
#define sci(n) scanf("%d",&n)
#define sci2(n,m) scanf("%d %d",&n,&m)
#define sci3(x,y,z) scanf("%d %d %d",&x,&y,&z)
#define scll(n) scanf("%lld",&n)
#define scll2(n,m) scanf("%lld, %lld",&n, &m)
#define scll3(n,m,p) scanf("%lld %lld %lld",&n, &m, &p)
#define MAX 1001
#define MOD 1000000007
#define INF 1000000000000000001
#define pi 3.1415926
#define pri(x) printf("%d\n",x)
#define prll(x) printf("%lld\n",x)
using namespace std;
typedef long long ll;

int main() {
	ll t,n,f,i,j,k,a,b,c;
	ll A[100005]={0};
	scll(t);
	ll R[100005]={0};
	long double p,q,r;
	r=0;
	for(i=0;i<t;i++){
		scll(A[i]);
		r+=A[i]*5;
	}
	for(i=0;i<t-1;i++){
		scll(B[i]);
		r+=B[i];
	}
	if(t==1)
	pri(A[0]*4);
	else{
	r-=A[t-1];
	a=r;
		cout<<a<<"\n";
	/*	r+=A[0]*4;
		
		for(i=1;i<t-1;i++){
			if(A[i]>A[i-1])
			r+=sqrt(B[i-1]*B[i-1]+(A[i]-A[i-1])*(A[i]-A[i-1]));
			else if(A[i-1]/2.0<A[i])
			r+=sqrt(B[i-1]*B[i-1]+(A[i]-A[i-1])*(A[i]-A[i-1]));
			else
			r+=sqrt(B[i-1]*B[i-1]+(A[i])*(A[i]));
			r+=A[i]*4;
		}
		r+
	*/
	}
	return 0;
}